pipeline:
  build:
    image: golang:1.8
    commands:
      - go get github.com/mattn/goveralls
      - goveralls -service=drone.io

services:
  postgres:
    image: postgres:9.4.5
    environment:
      - POSTGRES_USER=myapp